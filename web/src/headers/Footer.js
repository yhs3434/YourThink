import React from 'react';

const Footer = (props) => {
    const style = {
        wrap: {
            width: '100%'
        }
    }
    return (
        <div>
            footer
        </div>
    )
}

export default Footer;