import React from 'react';

const myLi = ({memoTitle, memoContent, published}) => {
    return (
        <div>
            tt
            <span>{memoTitle}</span>
            <span>{memoContent}</span>
            <span>{published}</span>
        </div>
    )
}

export default myLi;